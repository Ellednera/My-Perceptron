#!/usr/bin/perl

use strict;
use warnings;

use Test::More;

plan( skip_all => "Feature dropped due to API changes in Version 0.02" );
done_testing;

# train returns nerve file, while validate & test doesn't use it to do anything
# so can't combine them anymore :)

# besiyata d'shmaya




